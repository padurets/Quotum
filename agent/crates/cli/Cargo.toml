[package]
name = "agent-limits"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
agent-limits-core = { version = "0.1.0", path = "../core" }
clap = { version = "4.6.7", features = ["derive"] }
serde_json = "1.0.151"
